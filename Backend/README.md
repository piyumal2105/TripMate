# FastAPI Backend with Firebase

## Setup Instructions

### 1. Clone the Repository
```bash
git clone <repository-url>
cd backend

Install Dependencies - pip install -r requirements.txt

Run the Backend - uvicorn app.main:app --reload
